<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container">
		<div class="row row-cols-1 row-cols-md-3 g-4">
        {% for serie in series %}
            <div class="col">
				<div class="card shadow" style="width:18rem;">
					<img src="{{serie.cartel}}" class="card-img-top" />
					<div class="card-body">
						<h4 class="card-title">{{serie.titulo}}</h4>
						ESTRELLAS
						<h6>{{serie.plataforma}}</h6>
						<p class="card-text">{{serie.argumento}}</p>
						<a href="info.php?id=4" class="btn btn-dark btn-sm">Mas info.</a>
					</div>
				</div>
			</div>
        {% endfor %}
		</div>
	</div>
</body>
</html>